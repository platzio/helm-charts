apiVersion: v2
appVersion: "0.3.5"
version: "0.3.5"
description: A Helm chart for Platz.io
name: platzio
type: application
keywords:
  - helm
  - chart
  - cd
  - continuous deployment
icon: https://raw.githubusercontent.com/platzio/frontend/main/public/Logo.svg
sources:
  - https://github.com/platzio

dependencies:
  - name: postgresql
    repository: https://charts.bitnami.com/bitnami
    version: 11.1.29
    condition: postgresql.enabled

annotations:
  artifacthub.io/prerelease: "false"
  artifacthub.io/changes: |
    - kind: changed
      description: Upgraded postgresql subchart to 11.1.28
    - kind: fixed
      description: All workers now have resource definitions
    - kind: added
      description: The chart can create Certificate and Issuer for its Ingress
    - kind: changed
      description: Upgraded postgresql subchart to 11.1.29
